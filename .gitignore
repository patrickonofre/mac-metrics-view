.build/
.swiftpm/
DerivedData/
*.xcuserstate
xcuserdata/
.DS_Store
.claude/scheduled_tasks.lock

# Build/packaging artifacts (produced by the README build steps)
/build/
/dist/
/MacMetricsView.app/
/MacMetricsView-beta.zip
*.profraw

# Markdown docs stay local and are not version-controlled, except the
# project README at the repo root.
*.md
!/README.md
.agents/
.claude/
.codex/
.opencode/

# Internal docs and AI agent configs — kept local only
docs/product/
docs/ai/skills/*/agents/
skills
